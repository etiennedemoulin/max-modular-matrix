{
  "name": "max-modular-matrix",
  "version": "2.0.0",
  "description": "Max app for matrix easy life",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild ./src/modular-matrix.js  --bundle --format=cjs --platform=node --external:max-api --outfile=max/modular-matrix/javascript/modular.matrix.cjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json5": "^2.2.3"
  }
}
